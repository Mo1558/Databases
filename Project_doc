This project contains databases which are divided into main and clients. main is the one that contains the main tables of the project, not admin, auth and users, etc., and clients, and it contains the information of the clients. This project contains a set of apps [clients,helper,users,]. Clients It is the one that contains the information of the clients and their api, most of which are located in the helper and users, and it is the one that we modified the main user. There is also a folder for uploads and routers, uploads, which contains the things that are uploaded, and routers, which contains the things for each database 
